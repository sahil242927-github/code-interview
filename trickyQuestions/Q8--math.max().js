/*
 Math.max() => Return the number with the highest value
*/
console.log(Math.max(1, 2, 3)); // returns 2

/* Note: if you don't pass any number it will return -1  */
console.log(Math.max()); // return -1 or -infinity
